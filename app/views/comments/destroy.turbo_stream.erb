<%= turbo_stream.remove @comment %>
<%= turbo_stream.replace [dom_id(@comment.commentable), 'comment_size'] do %>
  <%= turbo_frame_tag [dom_id(@comment.commentable), 'comment_size'] do %>
     (<%= @comment_count %>) 
  <% end %>
<% end %>
<%= render_turbo_stream_flash_messages %>
